// =============================================================
//  chapters/10_tech_stack.typ — Глава 5. Стек технологий
//  мобильной разработки
// =============================================================

= Стек технологий мобильной разработки

Выбор стека технологий определяет не только скорость
разработки, но и долгосрочную поддерживаемость продукта,
доступность кадров и возможности интеграции с
корпоративной инфраструктурой. В данной главе рассматриваются
языки, фреймворки, инструменты CI/CD#footnote[CI/CD —
Continuous Integration / Continuous Delivery. Практика
автоматизации сборки, тестирования и доставки программного
обеспечения. Позволяет выпускать обновления приложений
быстро и с предсказуемым качеством.], тестирования
и безопасности, составляющие современный стек мобильной
разработки.

== Языки и платформенные фреймворки

=== Kotlin

Kotlin — основной язык разработки под Android и ключевой
язык KMP-экосистемы. Компилятор Kotlin (kotlinc) поддерживает
несколько бэкендов @kotlin-official:

— *Kotlin/JVM* — компиляция в JVM-байткод; стандарт
  для Android- и Десктоп-приложений и серверного Kotlin;

— *Kotlin/Native* — компиляция через LLVM в нативный
  машинный код; используется для iOS (arm64), Linux
  (x64, arm64), macOS, Windows; является основой
  для KMP на платформах без JVM;

— *Kotlin/JS* — компиляция в JavaScript; применяется
  для веб-таргетов в KMP-проектах;

— *Kotlin/Wasm* — компиляция в WebAssembly;
  экспериментальный таргет для Compose Multiplatform Web.

Для мобильной разработки ключевыми являются механизмы
языка: *Coroutines + Flow* для реактивного асинхронного
программирования, *Serialization* (`kotlinx.serialization`)
для работы с JSON/Protobuf без рефлексии, *Sealed classes*
для моделирования состояний UI.

=== Swift

Swift используется исключительно для нативной iOS-разработки
и серверных приложений (Vapor, Hummingbird). В контексте
KMP Swift взаимодействует с Kotlin через генерируемые
заголовки Objective-C: KMP-модуль компилируется в нативный
фреймворк `.xcframework`, который подключается в Xcode-проект
как обычная зависимость. Взаимодействие прозрачно для
Swift-разработчика: он вызывает Kotlin-классы как обычные
Swift-типы.

=== Dart

Dart — язык Google, разработанный специально под Flutter
@flutter-docs. Компилируется AOT в нативный код для production
и JIT для режима разработки (что обеспечивает Hot Reload —
обновление UI без перезапуска приложения). Dart не является
кросс-платформенным языком вне Flutter: экосистема
pub.dev замкнута на Flutter/Dart-приложения.

=== JavaScript / TypeScript

TypeScript — типизированное надмножество JavaScript —
используется в React Native и для разработки PWA. Статическая
типизация TypeScript решает основные проблемы надёжности
JavaScript-кода, однако не устраняет накладных расходов
JS-рантайма при выполнении.

=== C++ и Qt/QML

Для разработки приложений под ОС «Аврора» основным
инструментарием является Qt-фреймворк и декларативный
язык QML. C++ обеспечивает бизнес-логику и интеграцию
с системными библиотеками, QML — описание UI. Это
принципиально отличает «Аврору» от Android/iOS-платформ
и создаёт отдельную нишу компетенций, не пересекающуюся
с основными мобильными стеками.

#pagebreak()
== Кросс-платформенные фреймворки: детальное сравнение

=== Flutter vs. React Native vs. KMP/CMP

#figure(
  caption: [Детальное сравнение Flutter, React Native и KMP/CMP],
  kind: table,
  table(
    columns: (auto, 1fr, 1fr, 1fr),
    align: (left, center, center, center),
    table.header(
      [*Критерий*], [*Flutter*], [*React Native*], [*KMP / CMP*],
    ),
    [Стартовое время сборки],
      [Среднее (AOT)],
      [Быстрое (JS bundler)],
      [Среднее (Gradle + LLVM)],
    [Hot Reload],
      [Да (< 1 сек)],
      [Да (Fast Refresh)],
      [Частично (Android)],
    [Размер приложения],
      [+4–7 МБ (движок)],
      [+3–5 МБ (JS-рантайм)],
      [Минимальный оверхед],
    [Поддержка «Авроры»],
      [Нет],
      [Нет],
      [Да (Alpha) — см. ниже],
    [Типобезопасность],
      [Dart (строгая)],
      [TypeScript (опциональная)],
      [Kotlin (строгая)],
    [Доступ к нативным API],
      [Platform Channels (async)],
      [JSI (sync, New Arch.)],
      [Прямой (C-interop)],
    [Поддержка Coroutines],
      [Нет (собственный async)],
      [Нет (Promise/async-await)],
      [Да (native)],
    [Внедрение в enterprise (РФ)],
      [Активное],
      [Умеренное],
      [Растущее],
  )
)

#pagebreak()
== KMP и ОС «Аврора»: Aurora Multiplatform
#figure(
  image("../images/aurora-multiplatform-milestones.png", width: 80%),
  caption: [Этапы развития Aurora Multiplatform],
  kind: image,
)

Особого внимания заслуживает инициатива ОМП по поддержке
KMP для ОС «Аврора» — проект *Aurora Multiplatform*,
находящийся в стадии Alpha (версия 0.0.3) @aurora-kmp.
Это стратегически важное направление: оно позволяет
разработчикам, пишущим KMP-приложения под Android и iOS,
повторно использовать общий Kotlin-код для «Авроры»,
дописав лишь нативный UI на Qt/QML.

=== Архитектурная основа

ОС «Аврора», как и iOS, не имеет JVM. По этой причине
KMP доступен для «Авроры» через нативные таргеты
Kotlin/Native: `linuxX64` (эмулятор) и `linuxArm64`
(физическое устройство) @aurora-qt-bindings. Kotlin-код
компилируется через LLVM в нативную статическую или
динамическую C-библиотеку, которая затем подключается
к Qt/QML-приложению.

=== Компоненты Aurora Multiplatform

Проект включает четыре ключевых компонента @aurora-kmp:

*1. Aurora KInterop* — набор KMP-библиотек для взаимодействия
с системными API ОС «Аврора» @aurora-kinterop.
Библиотеки используют механизм *Kotlin C-interop*
(kotlinlang.org/docs/native-c-interop.html) для вызова
низкоуровневых C-библиотек платформы из Kotlin-кода.
Каждая библиотека самодостаточна и может использоваться
независимо. Публикуется через локальный Maven-репозиторий
командой `./gradlew publishToMavenLocal`.

*2. QtBindings* — плагин-кодогенератор, решающий
фундаментальную проблему интеграции KMP с Qt @aurora-qt-bindings.

При «голом» использовании Kotlin/Native возникает ряд
трудностей: генерируемый заголовочный C-файл содержит
тысячи строк всей бизнес-логики проекта; экспортируемый
интерфейс не поддерживает стандартные коллекции (List, Map);
ресурсы необходимо освобождать вручную через
`DisposeString` и `DisposeStablePointer`; Kotlin
Coroutines не экспортируются в C-интерфейс;
отсутствует объектно-ориентированный интерфейс классов
(в каждый метод передаётся указатель на объект).

QtBindings автоматизирует решение всех этих проблем:

— генерирует Qt C++-обёртки для верхнеуровневых
  Kotlin-классов и функций;
— обеспечивает поддержку Coroutines через `QFuture`
  (стандартный Qt-механизм асинхронности);
— автоматически конвертирует Kotlin `List` /
  `MutableList` в `QList`;
— автоматически управляет временем жизни объектов,
  устраняя необходимость ручного освобождения ресурсов.

*3. Compose Multiplatform для «Авроры»* — адаптация CMP
для Linux-таргета, на котором основана ОС «Аврора»
@aurora-cmp. CMP изначально не рассчитан на нативное
Linux-окружение, поэтому ОМП ведёт работу по адаптации
ключевых библиотек под `Linux`-таргет сборки.
Библиотеки KMP с доступной целевой сборкой `Linux`
в целом совместимы с «Авророй», однако часть CMP-библиотек
требует доработки — ОМП формирует и поддерживает
актуальный список поддерживаемых пакетов.

*4. BuildTools* — Gradle-плагин для управления
сборочной инфраструктурой @aurora-buildtools:

— создание sysroot#footnote[Sysroot — корневая файловая
  система целевой платформы, используемая при
  кросс-компиляции. Содержит заголовочные файлы и
  библиотеки целевой ОС, необходимые компилятору
  на машине разработчика.]-директории (поддерживаются
  macOS, Linux, Windows);
— сборка RPM#footnote[RPM (Red Hat Package Manager) —
  формат пакетов, используемый в дистрибутивах на базе
  RPM (RHEL, Fedora, ROSA). ОС «Аврора» использует RPM
  в качестве основного формата пакетов приложений.]-пакета
  с приложением;
— запуск приложения на эмуляторе и физическом устройстве.

#pagebreak()
=== Практическая схема KMP-проекта под «Аврору»

Типовой KMP-проект с поддержкой «Авроры» имеет
следующую структуру модулей:

```
project/
├── shared/                  # Общий KMP-модуль
│   ├── commonMain/          # Бизнес-логика (Domain + Data)
│   ├── androidMain/         # Android-специфика
│   ├── iosMain/             # iOS-специфика
│   └── linuxArm64Main/      # Аврора-специфика (KInterop)
├── androidApp/              # Android UI (Jetpack Compose)
├── iosApp/                  # iOS UI (SwiftUI)
└── auroraApp/               # Аврора UI (Qt/QML)
    └── src/
        ├── main.qml
        └── KmpBridge.cpp    # Qt-обёртки, генерируемые QtBindings
```

Таким образом, команда пишет бизнес-логику один раз
на Kotlin в `shared/commonMain`, а UI адаптирует
нативно для каждой платформы. «Аврора» при этом
включается в KMP-матрицу как полноправная цель сборки
наравне с Android и iOS.

== Backend и API

=== REST

*REST* (Representational State Transfer) — архитектурный
стиль взаимодействия, основанный на HTTP-протоколе.
Остаётся наиболее распространённым подходом для
мобильного backend благодаря простоте, широкой
инструментальной поддержке и хорошей кэшируемости.
Для Kotlin/Android стандартными библиотеками являются
*Ktor Client* (официальная KMP-библиотека JetBrains,
работающая на всех платформах KMP) и *Retrofit*
(Android-специфичная, не поддерживает iOS/«Аврору»).

=== GraphQL

*GraphQL* — язык запросов к API, разработанный Meta
в 2012 году. Ключевое преимущество перед REST —
клиент запрашивает ровно те поля, которые ему нужны,
исключая over-fetching#footnote[Over-fetching —
получение избыточных данных от сервера. Типичная
проблема REST: эндпоинт `/user` возвращает полный
объект пользователя, тогда как мобильному клиенту
нужны только имя и аватар.] и under-fetching.
Для мобильных приложений особенно значима подписка
(Subscription) через WebSocket — механизм
real-time обновлений без поллинга.

=== gRPC

*gRPC* — фреймворк Google для вызова удалённых процедур
на базе HTTP/2 и бинарной сериализации Protobuf.
Превосходит REST по производительности и объёму
передаваемых данных (бинарный формат в 3–10 раз
компактнее JSON). В мобильном контексте применяется
для высоконагруженных сценариев: стриминг данных
телеметрии, real-time синхронизация.

#figure(
  caption: [Сравнение подходов к API для мобильных приложений],
  kind: table,
  table(
    columns: (auto, 1fr, 1fr, 1fr),
    align: (left, center, center, center),
    table.header(
      [*Критерий*], [*REST*], [*GraphQL*], [*gRPC*],
    ),
    [Протокол],         [HTTP/1.1–2], [HTTP/1.1–2], [HTTP/2],
    [Формат данных],    [JSON/XML],   [JSON],        [Protobuf (бинарный)],
    [Типизация схемы],  [OpenAPI (опц.)], [Строгая], [Строгая (.proto)],
    [Поддержка стриминга], [Ограниченная], [Да (Subscription)], [Полная (bidirectional)],
    [Размер payload],   [Базовый],    [Оптимальный (no over-fetch)], [Минимальный],
    [Кэширование],      [Нативное (HTTP)], [Требует настройки], [Нет],
    [Порог входа],      [Низкий],     [Средний],    [Высокий],
  )
)

== CI/CD для мобильных приложений

Непрерывная интеграция и доставка в мобильной разработке
имеет специфику по сравнению с серверным ПО: необходима
подпись артефактов, взаимодействие с магазинами приложений
и тестирование на реальных устройствах.

=== Инструменты сборки и автоматизации

*Fastlane* @fastlane — Ruby-инструмент для автоматизации
рутинных задач iOS и Android разработки: сборка,
подпись, скриншоты, загрузка в App Store Connect
и Google Play Console. Включает более 250 готовых
действий (lanes) и поддерживает написание собственных.

*GitHub Actions* — облачная CI/CD-платформа с нативной
поддержкой macOS-раннеров (необходимы для сборки iOS).
Поддерживает матричные сборки: один workflow запускает
сборку Android и iOS параллельно.

*Bitrise* — CI/CD-платформа, специализированная на
мобильной разработке. Предоставляет готовые step-библиотеки
для Fastlane, тестирования на реальных устройствах
(Device Farm) и интеграции с App Store / Google Play.

*GitLab CI* — широко применяется в корпоративном секторе
(в том числе российском) благодаря возможности
self-hosted развёртывания, что критично для проектов
с требованиями к размещению исходного кода на территории РФ.
ОМП размещает Aurora Multiplatform на mos.hub —
российской альтернативе GitLab.

=== Типовой CI/CD-пайплайн

Типовой пайплайн мобильного приложения включает следующие
стадии:

+ *Lint + Static Analysis* — проверка стиля кода
  (ktlint, detekt для Kotlin; SwiftLint для Swift);
  запускается на каждый Pull Request, блокирует merge
  при нарушениях;

+ *Unit Tests* — быстрые тесты бизнес-логики без
  зависимости от устройства; время выполнения — секунды;

+ *Build* — сборка debug и release артефактов;
  для release обязательна подпись (Keystore для Android,
  Apple Distribution Certificate для iOS);

+ *UI Tests / Instrumented Tests* — запуск на эмуляторе
  или реальном устройстве; наиболее долгая стадия
  (минуты);

+ *Distribution* — загрузка артефакта в тестовый
  дистрибутив (Firebase App Distribution, TestFlight)
  или в продуктовый магазин.

== Тестирование мобильных приложений

=== Пирамида тестирования

Классическая пирамида тестирования для мобильных приложений
предполагает следующее соотношение: большинство тестов —
быстрые unit-тесты, меньшая часть — интеграционные,
и минимальная — сквозные E2E#footnote[E2E (End-to-End)
тестирование — тестирование всего пользовательского
пути от UI до backend. Медленнее и дороже unit-тестов,
но проверяет реальное взаимодействие компонентов.]-тесты.

=== Unit-тестирование

*JUnit 5* + *MockK* (Kotlin) и *XCTest* + *Quick/Nimble*
(Swift) — стандартные фреймворки для unit-тестирования
бизнес-логики. В KMP-проектах unit-тесты бизнес-логики
в `commonMain` пишутся один раз с использованием
`kotlin.test` и запускаются на всех целевых платформах.

=== UI-тестирование

*Espresso* (Android) — официальный фреймворк Google
для тестирования UI Android-приложений. Синхронизируется
с UI-потоком приложения, что исключает нестабильные
тесты (flaky tests) из-за задержек.

*XCUITest* (iOS) — официальный Apple-фреймворк для
UI-тестирования. Работает «снаружи» приложения через
accessibility API, что приближает тесты к реальному
пользовательскому взаимодействию.

*Compose Testing* — API для тестирования
Jetpack Compose UI: `composeTestRule.onNodeWithTag()`
позволяет находить компоненты по семантическим тегам
без привязки к конкретной реализации.

=== E2E-тестирование

*Detox* — фреймворк Wix для E2E-тестирования React
Native приложений. Работает на реальных устройствах
и эмуляторах, поддерживает Android и iOS.

*Maestro* — декларативный YAML-фреймворк для E2E-тестов,
набирающий популярность благодаря простоте написания
сценариев и нативной поддержке как Android, так и iOS
без написания кода.

== Безопасность мобильных приложений

=== Обфускация кода

*ProGuard / R8* (Android) — инструменты минификации
и обфускации байткода. R8 является официальным
компилятором Kotlin/Java начиная с Android Gradle
Plugin 3.4 и совмещает оптимизацию и обфускацию
в одном проходе, что сокращает размер DEX-файлов
на 10–30%.

Важно понимать, что обфускация не является
средством защиты: она лишь увеличивает стоимость
реверс-инжиниринга, но не исключает его. Kotlin/Native
(KMP) обеспечивает значительно более высокий уровень
защиты кода: нативный бинарный код существенно сложнее
для анализа, чем JVM-байткод.

=== Certificate Pinning

*Certificate Pinning* — механизм защиты от атак
типа Man-in-the-Middle#footnote[MITM (Man-in-the-Middle)
— атака, при которой злоумышленник перехватывает
трафик между клиентом и сервером, подставляя
собственный TLS-сертификат. Certificate Pinning
исключает доверие к произвольным CA и принимает
только заранее известный сертификат сервера.]:
приложение принимает только заранее определённый
сертификат (или его хэш — public key pin) сервера,
игнорируя любые другие, даже подписанные доверенным CA.

В Android реализуется через `network_security_config.xml`
(декларативно) или через OkHttp `CertificatePinner`
(программно). В iOS — через `URLSession` c кастомным
`URLSessionDelegate`. В KMP/Ktor — через модуль
`ktor-client-cio` с настройкой `TLSConfig`.

=== Хранение секретов

Мобильное устройство — физически доступная злоумышленнику
среда, поэтому хранение секретных данных требует
особого внимания.

#figure(
  caption: [Подходы к хранению секретов на мобильном устройстве],
  kind: table,
  table(
    columns: (0.5fr, 1fr, 1fr),
    align: (left, center, center),
    table.header(
      [*Данные*], [*Android*], [*iOS*],
    ),
    [Криптографические ключи],
      [Android Keystore System],
      [Secure Enclave / Keychain],
    [Токены авторизации],
      [EncryptedSharedPreferences],
      [Keychain Services],
    [Сертификаты],
      [KeyStore + сертификат X.509],
      [Keychain (идентификаторы)],
    [Конфигурация],
      [BuildConfig (не для секретов!) / сервер],
      [Info.plist (не для секретов!) / сервер],
    [Биометрия],
      [BiometricPrompt + Keystore],
      [LocalAuthentication + Secure Enclave],
  )
)

Принципиально важно: API-ключи, секретные токены
и приватные сертификаты *не должны* храниться в коде
приложения или ресурсах — они подлежат обязательному
извлечению при статическом анализе APK/IPA. Правильный
подход — хранить секреты на сервере и выдавать
приложению короткоживущие токены после аутентификации.

=== OWASP Mobile Top 10

Стандартом описания угроз безопасности мобильных
приложений является *OWASP Mobile Top 10* — перечень
десяти наиболее распространённых категорий уязвимостей.
К числу наиболее актуальных для enterprise-приложений
относятся: небезопасная аутентификация, недостаточная
криптография, небезопасное хранение данных и
небезопасная сетевая коммуникация. В российском контексте
дополнительным требованием для приложений, работающих
с персональными данными, является соответствие
Федеральному закону № 152-ФЗ «О персональных данных»
в части хранения и обработки данных на серверах,
физически расположенных на территории РФ.
